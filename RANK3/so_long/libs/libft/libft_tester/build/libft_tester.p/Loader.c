/* Loader.c generated by valac 0.56.0, the Vala compiler
 * generated from Loader.vala, do not modify */

#include <glib.h>
#include <glib-object.h>
#include "dlfcn.h"
#include <stdlib.h>
#include <string.h>
#include <gobject/gvaluecollector.h>

#if !defined(VALA_EXTERN)
#if defined(_MSC_VER)
#define VALA_EXTERN __declspec(dllexport) extern
#elif __GNUC__ >= 4
#define VALA_EXTERN __attribute__((visibility("default"))) extern
#else
#define VALA_EXTERN extern
#endif
#endif

#define TYPE_ERR_LOADER (err_loader_get_type ())

#define TYPE_LOADER (loader_get_type ())
#define LOADER(obj) (G_TYPE_CHECK_INSTANCE_CAST ((obj), TYPE_LOADER, Loader))
#define LOADER_CLASS(klass) (G_TYPE_CHECK_CLASS_CAST ((klass), TYPE_LOADER, LoaderClass))
#define IS_LOADER(obj) (G_TYPE_CHECK_INSTANCE_TYPE ((obj), TYPE_LOADER))
#define IS_LOADER_CLASS(klass) (G_TYPE_CHECK_CLASS_TYPE ((klass), TYPE_LOADER))
#define LOADER_GET_CLASS(obj) (G_TYPE_INSTANCE_GET_CLASS ((obj), TYPE_LOADER, LoaderClass))

typedef struct _Loader Loader;
typedef struct _LoaderClass LoaderClass;
typedef struct _LoaderPrivate LoaderPrivate;
#define _dlclose0(var) ((var == NULL) ? NULL : (var = (dlclose (var), NULL)))
#define _g_free0(var) (var = (g_free (var), NULL))
#define _loader_unref0(var) ((var == NULL) ? NULL : (var = (loader_unref (var), NULL)))
typedef struct _ParamSpecLoader ParamSpecLoader;

typedef enum  {
	ERR_LOADER_NOT_FOUND,
	ERR_LOADER_SYMBOL_NOTFOUND
} ErrLoader;
#define ERR_LOADER err_loader_quark ()

struct _Loader {
	GTypeInstance parent_instance;
	volatile int ref_count;
	LoaderPrivate * priv;
};

struct _LoaderClass {
	GTypeClass parent_class;
	void (*finalize) (Loader *self);
};

struct _LoaderPrivate {
	GMutex mutex;
	void* handle;
};

struct _ParamSpecLoader {
	GParamSpec parent_instance;
};

static gint Loader_private_offset;
static gpointer loader_parent_class = NULL;

VALA_EXTERN GQuark err_loader_quark (void);
GType err_loader_get_type (void) G_GNUC_CONST ;
VALA_EXTERN gpointer loader_ref (gpointer instance);
VALA_EXTERN void loader_unref (gpointer instance);
VALA_EXTERN GParamSpec* param_spec_loader (const gchar* name,
                               const gchar* nick,
                               const gchar* blurb,
                               GType object_type,
                               GParamFlags flags);
VALA_EXTERN void value_set_loader (GValue* value,
                       gpointer v_object);
VALA_EXTERN void value_take_loader (GValue* value,
                        gpointer v_object);
VALA_EXTERN gpointer value_get_loader (const GValue* value);
VALA_EXTERN GType loader_get_type (void) G_GNUC_CONST ;
G_DEFINE_AUTOPTR_CLEANUP_FUNC (Loader, loader_unref)
VALA_EXTERN Loader* loader_new (const gchar* library,
                    GError** error);
VALA_EXTERN Loader* loader_construct (GType object_type,
                          const gchar* library,
                          GError** error);
VALA_EXTERN void* loader_symbol (Loader* self,
                     const gchar* name,
                     GError** error);
static void loader_finalize (Loader * obj);
static GType loader_get_type_once (void);
static void _vala_clear_GMutex (GMutex * mutex);
static void _vala_clear_GRecMutex (GRecMutex * mutex);
static void _vala_clear_GRWLock (GRWLock * mutex);
static void _vala_clear_GCond (GCond * mutex);

GQuark
err_loader_quark (void)
{
	return g_quark_from_static_string ("err-loader-quark");
}

static GType
err_loader_get_type_once (void)
{
	static const GEnumValue values[] = {{ERR_LOADER_NOT_FOUND, "ERR_LOADER_NOT_FOUND", "not-found"}, {ERR_LOADER_SYMBOL_NOTFOUND, "ERR_LOADER_SYMBOL_NOTFOUND", "symbol-notfound"}, {0, NULL, NULL}};
	GType err_loader_type_id;
	err_loader_type_id = g_enum_register_static ("ErrLoader", values);
	return err_loader_type_id;
}

GType
err_loader_get_type (void)
{
	static volatile gsize err_loader_type_id__once = 0;
	if (g_once_init_enter (&err_loader_type_id__once)) {
		GType err_loader_type_id;
		err_loader_type_id = err_loader_get_type_once ();
		g_once_init_leave (&err_loader_type_id__once, err_loader_type_id);
	}
	return err_loader_type_id__once;
}

static inline gpointer
loader_get_instance_private (Loader* self)
{
	return G_STRUCT_MEMBER_P (self, Loader_private_offset);
}

static gchar*
module_error (void* self)
{
	gchar* _tmp0_;
	gchar* result;
	g_return_val_if_fail (self != NULL, NULL);
	_tmp0_ = dlerror ();
	result = _tmp0_;
	return result;
}

static const gchar*
string_to_string (const gchar* self)
{
	const gchar* result;
	g_return_val_if_fail (self != NULL, NULL);
	result = self;
	return result;
}

Loader*
loader_construct (GType object_type,
                  const gchar* library,
                  GError** error)
{
	Loader* self = NULL;
	void* _tmp0_;
	gboolean _tmp1_ = FALSE;
	void* _tmp2_;
	GError* _inner_error0_ = NULL;
	g_return_val_if_fail (library != NULL, NULL);
	self = (Loader*) g_type_create_instance (object_type);
	_vala_clear_GMutex (&self->priv->mutex);
	g_mutex_init (&self->priv->mutex);
	_tmp0_ = dlopen (library, RTLD_LAZY);
	_dlclose0 (self->priv->handle);
	self->priv->handle = _tmp0_;
	_tmp2_ = self->priv->handle;
	if (_tmp2_ == NULL) {
		void* _tmp3_;
		gchar* _tmp4_;
		gchar* _tmp5_;
		_tmp3_ = self->priv->handle;
		_tmp4_ = module_error (_tmp3_);
		_tmp5_ = _tmp4_;
		_tmp1_ = _tmp5_ != NULL;
		_g_free0 (_tmp5_);
	} else {
		_tmp1_ = FALSE;
	}
	if (_tmp1_) {
		const gchar* _tmp6_;
		gchar* _tmp7_;
		gchar* _tmp8_;
		GError* _tmp9_;
		GError* _tmp10_;
		_tmp6_ = string_to_string (library);
		_tmp7_ = g_strconcat ("Can't found ", _tmp6_, NULL);
		_tmp8_ = _tmp7_;
		_tmp9_ = g_error_new_literal (ERR_LOADER, ERR_LOADER_NOT_FOUND, _tmp8_);
		_tmp10_ = _tmp9_;
		_g_free0 (_tmp8_);
		_inner_error0_ = _tmp10_;
		if (_inner_error0_->domain == ERR_LOADER) {
			g_propagate_error (error, _inner_error0_);
			_loader_unref0 (self);
			return NULL;
		} else {
			g_critical ("file %s: line %d: uncaught error: %s (%s, %d)", __FILE__, __LINE__, _inner_error0_->message, g_quark_to_string (_inner_error0_->domain), _inner_error0_->code);
			g_clear_error (&_inner_error0_);
			return NULL;
		}
	}
	return self;
}

Loader*
loader_new (const gchar* library,
            GError** error)
{
	return loader_construct (TYPE_LOADER, library, error);
}

void*
loader_symbol (Loader* self,
               const gchar* name,
               GError** error)
{
	void* ptr = NULL;
	void* _tmp0_;
	void* _tmp1_;
	void* _tmp2_;
	const gchar* _tmp4_;
	gchar* _tmp5_;
	gchar* _tmp6_;
	GError* _tmp7_;
	GError* _tmp8_;
	GError* _inner_error0_ = NULL;
	void* result;
	g_return_val_if_fail (self != NULL, NULL);
	g_return_val_if_fail (name != NULL, NULL);
	g_mutex_lock (&self->priv->mutex);
	_tmp0_ = self->priv->handle;
	_tmp1_ = dlsym (_tmp0_, name);
	ptr = _tmp1_;
	_tmp2_ = ptr;
	if (_tmp2_ != NULL) {
		void* _tmp3_;
		g_mutex_unlock (&self->priv->mutex);
		_tmp3_ = ptr;
		result = _tmp3_;
		return result;
	}
	g_mutex_unlock (&self->priv->mutex);
	_tmp4_ = string_to_string (name);
	_tmp5_ = g_strconcat (_tmp4_, " not found", NULL);
	_tmp6_ = _tmp5_;
	_tmp7_ = g_error_new_literal (ERR_LOADER, ERR_LOADER_SYMBOL_NOTFOUND, _tmp6_);
	_tmp8_ = _tmp7_;
	_g_free0 (_tmp6_);
	_inner_error0_ = _tmp8_;
	if (_inner_error0_->domain == ERR_LOADER) {
		g_propagate_error (error, _inner_error0_);
		return NULL;
	} else {
		g_critical ("file %s: line %d: uncaught error: %s (%s, %d)", __FILE__, __LINE__, _inner_error0_->message, g_quark_to_string (_inner_error0_->domain), _inner_error0_->code);
		g_clear_error (&_inner_error0_);
		return NULL;
	}
}

static void
value_loader_init (GValue* value)
{
	value->data[0].v_pointer = NULL;
}

static void
value_loader_free_value (GValue* value)
{
	if (value->data[0].v_pointer) {
		loader_unref (value->data[0].v_pointer);
	}
}

static void
value_loader_copy_value (const GValue* src_value,
                         GValue* dest_value)
{
	if (src_value->data[0].v_pointer) {
		dest_value->data[0].v_pointer = loader_ref (src_value->data[0].v_pointer);
	} else {
		dest_value->data[0].v_pointer = NULL;
	}
}

static gpointer
value_loader_peek_pointer (const GValue* value)
{
	return value->data[0].v_pointer;
}

static gchar*
value_loader_collect_value (GValue* value,
                            guint n_collect_values,
                            GTypeCValue* collect_values,
                            guint collect_flags)
{
	if (collect_values[0].v_pointer) {
		Loader * object;
		object = collect_values[0].v_pointer;
		if (object->parent_instance.g_class == NULL) {
			return g_strconcat ("invalid unclassed object pointer for value type `", G_VALUE_TYPE_NAME (value), "'", NULL);
		} else if (!g_value_type_compatible (G_TYPE_FROM_INSTANCE (object), G_VALUE_TYPE (value))) {
			return g_strconcat ("invalid object type `", g_type_name (G_TYPE_FROM_INSTANCE (object)), "' for value type `", G_VALUE_TYPE_NAME (value), "'", NULL);
		}
		value->data[0].v_pointer = loader_ref (object);
	} else {
		value->data[0].v_pointer = NULL;
	}
	return NULL;
}

static gchar*
value_loader_lcopy_value (const GValue* value,
                          guint n_collect_values,
                          GTypeCValue* collect_values,
                          guint collect_flags)
{
	Loader ** object_p;
	object_p = collect_values[0].v_pointer;
	if (!object_p) {
		return g_strdup_printf ("value location for `%s' passed as NULL", G_VALUE_TYPE_NAME (value));
	}
	if (!value->data[0].v_pointer) {
		*object_p = NULL;
	} else if (collect_flags & G_VALUE_NOCOPY_CONTENTS) {
		*object_p = value->data[0].v_pointer;
	} else {
		*object_p = loader_ref (value->data[0].v_pointer);
	}
	return NULL;
}

GParamSpec*
param_spec_loader (const gchar* name,
                   const gchar* nick,
                   const gchar* blurb,
                   GType object_type,
                   GParamFlags flags)
{
	ParamSpecLoader* spec;
	g_return_val_if_fail (g_type_is_a (object_type, TYPE_LOADER), NULL);
	spec = g_param_spec_internal (G_TYPE_PARAM_OBJECT, name, nick, blurb, flags);
	G_PARAM_SPEC (spec)->value_type = object_type;
	return G_PARAM_SPEC (spec);
}

gpointer
value_get_loader (const GValue* value)
{
	g_return_val_if_fail (G_TYPE_CHECK_VALUE_TYPE (value, TYPE_LOADER), NULL);
	return value->data[0].v_pointer;
}

void
value_set_loader (GValue* value,
                  gpointer v_object)
{
	Loader * old;
	g_return_if_fail (G_TYPE_CHECK_VALUE_TYPE (value, TYPE_LOADER));
	old = value->data[0].v_pointer;
	if (v_object) {
		g_return_if_fail (G_TYPE_CHECK_INSTANCE_TYPE (v_object, TYPE_LOADER));
		g_return_if_fail (g_value_type_compatible (G_TYPE_FROM_INSTANCE (v_object), G_VALUE_TYPE (value)));
		value->data[0].v_pointer = v_object;
		loader_ref (value->data[0].v_pointer);
	} else {
		value->data[0].v_pointer = NULL;
	}
	if (old) {
		loader_unref (old);
	}
}

void
value_take_loader (GValue* value,
                   gpointer v_object)
{
	Loader * old;
	g_return_if_fail (G_TYPE_CHECK_VALUE_TYPE (value, TYPE_LOADER));
	old = value->data[0].v_pointer;
	if (v_object) {
		g_return_if_fail (G_TYPE_CHECK_INSTANCE_TYPE (v_object, TYPE_LOADER));
		g_return_if_fail (g_value_type_compatible (G_TYPE_FROM_INSTANCE (v_object), G_VALUE_TYPE (value)));
		value->data[0].v_pointer = v_object;
	} else {
		value->data[0].v_pointer = NULL;
	}
	if (old) {
		loader_unref (old);
	}
}

static void
loader_class_init (LoaderClass * klass,
                   gpointer klass_data)
{
	loader_parent_class = g_type_class_peek_parent (klass);
	((LoaderClass *) klass)->finalize = loader_finalize;
	g_type_class_adjust_private_offset (klass, &Loader_private_offset);
}

static void
loader_instance_init (Loader * self,
                      gpointer klass)
{
	self->priv = loader_get_instance_private (self);
	self->ref_count = 1;
}

static void
loader_finalize (Loader * obj)
{
	Loader * self;
	self = G_TYPE_CHECK_INSTANCE_CAST (obj, TYPE_LOADER, Loader);
	g_signal_handlers_destroy (self);
	_vala_clear_GMutex (&self->priv->mutex);
	_dlclose0 (self->priv->handle);
}

static GType
loader_get_type_once (void)
{
	static const GTypeValueTable g_define_type_value_table = { value_loader_init, value_loader_free_value, value_loader_copy_value, value_loader_peek_pointer, "p", value_loader_collect_value, "p", value_loader_lcopy_value };
	static const GTypeInfo g_define_type_info = { sizeof (LoaderClass), (GBaseInitFunc) NULL, (GBaseFinalizeFunc) NULL, (GClassInitFunc) loader_class_init, (GClassFinalizeFunc) NULL, NULL, sizeof (Loader), 0, (GInstanceInitFunc) loader_instance_init, &g_define_type_value_table };
	static const GTypeFundamentalInfo g_define_type_fundamental_info = { (G_TYPE_FLAG_CLASSED | G_TYPE_FLAG_INSTANTIATABLE | G_TYPE_FLAG_DERIVABLE | G_TYPE_FLAG_DEEP_DERIVABLE) };
	GType loader_type_id;
	loader_type_id = g_type_register_fundamental (g_type_fundamental_next (), "Loader", &g_define_type_info, &g_define_type_fundamental_info, 0);
	Loader_private_offset = g_type_add_instance_private (loader_type_id, sizeof (LoaderPrivate));
	return loader_type_id;
}

GType
loader_get_type (void)
{
	static volatile gsize loader_type_id__once = 0;
	if (g_once_init_enter (&loader_type_id__once)) {
		GType loader_type_id;
		loader_type_id = loader_get_type_once ();
		g_once_init_leave (&loader_type_id__once, loader_type_id);
	}
	return loader_type_id__once;
}

gpointer
loader_ref (gpointer instance)
{
	Loader * self;
	self = instance;
	g_atomic_int_inc (&self->ref_count);
	return instance;
}

void
loader_unref (gpointer instance)
{
	Loader * self;
	self = instance;
	if (g_atomic_int_dec_and_test (&self->ref_count)) {
		LOADER_GET_CLASS (self)->finalize (self);
		g_type_free_instance ((GTypeInstance *) self);
	}
}

static void
_vala_clear_GMutex (GMutex * mutex)
{
	GMutex zero_mutex = { 0 };
	if (memcmp (mutex, &zero_mutex, sizeof (GMutex))) {
		g_mutex_clear (mutex);
		memset (mutex, 0, sizeof (GMutex));
	}
}

static void
_vala_clear_GRecMutex (GRecMutex * mutex)
{
	GRecMutex zero_mutex = { 0 };
	if (memcmp (mutex, &zero_mutex, sizeof (GRecMutex))) {
		g_rec_mutex_clear (mutex);
		memset (mutex, 0, sizeof (GRecMutex));
	}
}

static void
_vala_clear_GRWLock (GRWLock * mutex)
{
	GRWLock zero_mutex = { 0 };
	if (memcmp (mutex, &zero_mutex, sizeof (GRWLock))) {
		g_rw_lock_clear (mutex);
		memset (mutex, 0, sizeof (GRWLock));
	}
}

static void
_vala_clear_GCond (GCond * mutex)
{
	GCond zero_mutex = { 0 };
	if (memcmp (mutex, &zero_mutex, sizeof (GCond))) {
		g_cond_clear (mutex);
		memset (mutex, 0, sizeof (GCond));
	}
}

